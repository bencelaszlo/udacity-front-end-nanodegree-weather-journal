# Udacity Front-End Nanodegree - Weather-Journal Project

An asynchronous web app that uses Web API and user data to dynamically update the UI.

Default port: `3000`

Install dependencies: `npm install`

Start: `npm start`
